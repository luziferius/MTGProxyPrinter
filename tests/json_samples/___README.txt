This directory contains sample JSON output returned from the Scryfall API.
Each document represents a specific card type and is used to check the parser against it.
These types are regular cards, double-faced cards, split cards and similar.
